// Ejemplo 4: Extraer una porción de un string.

// Crea un string y utiliza un método para extraer una porción de él.

let str = "Hello world";

let subStr = str.slice(0, 5);

console.log(str);
console.log(subStr);
