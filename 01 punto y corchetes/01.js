// Ejemplo 1: Acceder a una propiedad por nombre fijo

// Crea un objeto llamado persona con las propiedades
// nombre y edad. Luego, muestra el nombre de la persona 
// utilizando tanto notación de punto como notación de 
// corchetes.

let persona = {
  nombre: "Juan",
  edad: 30,
};

console.log(persona.nombre);
console.log(persona["nombre"]);
