// Ejemplo 4: Acceder a una propiedad con espacio en el nombre

// Crea un objeto llamado pelicula con una propiedad cuyo nombre
// contiene un espacio, por ejemplo, "nombre original". Luego, 
// muestra el valor de esa propiedad utilizando notación de corchetes.

let pelicula = {
  "nombre original": "Batman",
};

console.log(pelicula["nombre original"]);
