// Ejemplo 1: Iterar sobre un arreglo de números y mostrarlos en la consola.

// Crea un arreglo de números y utiliza el método forEach
// para mostrar cada número en la consola.

let numeros = [1, 2, 3, 4, 5];

numeros.forEach((numero) => {
  console.log(numero);
});
