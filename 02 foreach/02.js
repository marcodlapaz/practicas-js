// Ejemplo 2: Iterar sobre un arreglo de nombres y saludar a cada persona.

// Crea un arreglo de nombres y utiliza el método forEach para saludar
// a cada persona en la consola.

let nombres = ["Marco", "Antonio", "Carmen"];

nombres.forEach((nombre) => {
  console.log(`Hola ${nombre} 👋`);
});
